apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      # Path where dashboard JSON files are mounted inside the container
      path: /etc/grafana/dashboards
